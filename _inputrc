# case-insensitive completion
set completion-ignore-case on

# show multiple completion matches immediately
set show-all-if-unmodified on

# Ctrl-k clears the current line
"\C-k": kill-whole-line

$if Bash
  # expand !!, !* and friends by pressing Space
  Space: magic-space
$endif

# vim:ft=readline
